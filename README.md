# KNearestNeighbor-Classifier
This project implements a K-Nearest Neighbors classifier to predict wine types using the scikit-learn Wine dataset. KNN is a simple, instance-based learning algorithm that classifies samples by finding the K closest training examples and taking a majority vote among them.
